#inolude <servo.h>
Servo servo _0 ;
const int servoPin = 7;
const int ledPin = 2;

void setup()
{
  pinMode(ledPin, OUTPUT) ;
  servo_0, attach (servoPin) ;
  servo_0, write(pos) ;
}

void loop()
{
  digitalWritte(2,HIGH);
  for (pos = 0; pos ,= 90 ; pos += 1)
      servo_0.write(pos) ;
      delay(15);
}
   delay(2000)
  }				
